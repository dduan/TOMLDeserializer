matrix:
  include:
    - os: osx
      env: ACTION=CocoaPods
    - os: osx
      env: ACTION=SwiftPM
    - os: linux
      language: generic
      dist: trusty
      sudo: required
      services:
        - docker
      script:
        - make test-docker

language: objective-c
osx_image: xcode10.1

script:
  - make install-$ACTION && make test-$ACTION
